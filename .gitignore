# Python
__pycache__/
*.py[cod]
*.egg-info/
.pytest_cache/
.venv/
venv/
.env/

# Build artifacts
build/
dist/
*.whl

# Local Spark (created by pytest / local runs — never commit)
spark-warehouse/
metastore_db/
derby.log

# Generated seed artifacts (regenerate via simulators.write_all_artifacts)
# NOTE: tests/, sample_service/, sample_stream/ and docs/ are REAL SOURCE — never ignore them.
mongo_import/
sample_source/
*.seed.sql

# Runtime output from tools/check_runs.py (the monitor workflow) — never commit
failures.txt

# Local agent/session config
.claude/

# OS / editor
.DS_Store
.idea/
.vscode/
# Onboarding deck + its generator — keep local, do NOT commit (per request)
*.pptx
tools/build_onboarding_pptx.py
